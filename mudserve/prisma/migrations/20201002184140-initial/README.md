# Migration `20201002184140-initial`

This migration has been generated by Kevin Jolley at 10/2/2020, 1:41:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL
)

CREATE TABLE "Inventory" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "description" TEXT NOT NULL,
    "userId" INTEGER,
    "amount" INTEGER NOT NULL,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Item" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "description" TEXT NOT NULL,
    "userId" INTEGER,
    "amount" INTEGER NOT NULL,
    "inventoryId" INTEGER,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("inventoryId") REFERENCES "Inventory"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201002184140-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,36 @@
+datasource db {
+  provider = "sqlite"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        Int         @id @default(autoincrement())
+  name      String
+  email     String      @unique
+  password  String
+  Inventory Inventory[]
+  Item      Item[]
+}
+
+model Inventory {
+  id          Int    @id
+  description String
+  ownedBy     User?  @relation(fields: [userId], references: [id])
+  userId      Int?
+  amount      Int
+  items       Item[]
+}
+
+model Item {
+  id          Int        @id
+  description String
+  userId      Int?
+  amount      Int
+  User        User?      @relation(fields: [userId], references: [id])
+  Inventory   Inventory? @relation(fields: [inventoryId], references: [id])
+  inventoryId Int?
+}
```


