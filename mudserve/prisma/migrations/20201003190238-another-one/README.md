# Migration `20201003190238-another-one`

This migration has been generated by Kevin Jolley at 10/3/2020, 2:02:38 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Inventory" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "description" TEXT NOT NULL,
    "amount" INTEGER NOT NULL
);
INSERT INTO "new_Inventory" ("id", "description", "amount") SELECT "id", "description", "amount" FROM "Inventory";
DROP TABLE "Inventory";
ALTER TABLE "new_Inventory" RENAME TO "Inventory";
CREATE TABLE "new_Item" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL DEFAULT 'Item',
    "description" TEXT NOT NULL,
    "amount" INTEGER NOT NULL,
    "inventoryId" INTEGER,

    FOREIGN KEY ("inventoryId") REFERENCES "Inventory"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Item" ("id", "description", "amount", "inventoryId", "name") SELECT "id", "description", "amount", "inventoryId", "name" FROM "Item";
DROP TABLE "Item";
ALTER TABLE "new_Item" RENAME TO "Item";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

PRAGMA foreign_keys=off;
DROP TABLE "User";
PRAGMA foreign_keys=on
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201003181131-item-name..20201003190238-another-one
--- datamodel.dml
+++ datamodel.dml
@@ -1,37 +1,24 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
-model User {
-  id        Int         @id @default(autoincrement())
-  name      String
-  email     String      @unique
-  password  String
-  Inventory Inventory[]
-  Item      Item[]
-}
-
 model Inventory {
-  id          Int    @id
+  id          Int    @id @default(autoincrement())
   description String
-  ownedBy     User?  @relation(fields: [userId], references: [id])
-  userId      Int?
   amount      Int
   items       Item[]
 }
 model Item {
-  id          Int        @id
+  id          Int        @id @default(autoincrement())
   name        String     @default("Item")
   description String
-  userId      Int?
   amount      Int
-  User        User?      @relation(fields: [userId], references: [id])
   Inventory   Inventory? @relation(fields: [inventoryId], references: [id])
   inventoryId Int?
 }
```


